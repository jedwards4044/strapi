{
  "collectionName": "components_item_banner_items",
  "info": {
    "displayName": "Banner Item",
    "description": ""
  },
  "options": {},
  "attributes": {
    "headline": {
      "type": "string"
    },
    "subheader": {
      "type": "string"
    },
    "image": {
      "type": "media",
      "multiple": true,
      "required": false,
      "allowedTypes": ["images", "files", "videos", "audios"]
    },
    "buttons": {
      "type": "component",
      "repeatable": true,
      "component": "buttons.item-button",
      "required": false
    },
    "desc": {
      "type": "text"
    },
    "headerTagH1": {
      "type": "boolean",
      "default": false
    },
    "isFeatured": {
      "type": "boolean",
      "default": false
    },
    "disabled": {
      "type": "boolean",
      "default": false
    },
    "align": {
      "type": "enumeration",
      "enum": ["left", "right", "center"]
    },
    "headSize": {
      "type": "enumeration",
      "enum": ["XS", "SM", "MD", "LG", "XL"]
    },
    "descSize": {
      "type": "enumeration",
      "enum": ["XS", "SM", "MD", "LG", "XL"]
    }
  }
}
